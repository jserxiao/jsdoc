# 构建优化策略

## 速度优化

### 优化策略对比

| 策略 | 说明 | 适用工具 | 效果 |
|------|------|----------|------|
| **持久化缓存** | 缓存编译结果，增量构建 | Webpack、Vite | ⭐⭐⭐⭐⭐ |
| **并行构建** | 多线程/多进程编译 | thread-loader、HappyPack | ⭐⭐⭐⭐ |
| **缩小范围** | include/exclude 减少处理文件 | 所有工具 | ⭐⭐⭐⭐ |
| **使用 SWC/esbuild** | 替代 Babel 提速 | Webpack、Vite | ⭐⭐⭐⭐⭐ |

### 持久化缓存

**Webpack 配置**
```javascript
module.exports = {
  cache: {
    type: 'filesystem',
    buildDependencies: {
      config: [__filename]
    }
  }
}
```

**Babel Loader 缓存**
```javascript
{
  loader: 'babel-loader',
  options: {
    cacheDirectory: true
  }
}
```

### 并行构建

**thread-loader（Webpack）**
```javascript
module.exports = {
  module: {
    rules: [
      {
        test: /\.js$/,
        use: [
          {
            loader: 'thread-loader',
            options: {
              workers: require('os').cpus().length - 1
            }
          },
          'babel-loader'
        ]
      }
    ]
  }
}
```

### 使用 SWC 替代 Babel

```javascript
// webpack.config.js
module.exports = {
  module: {
    rules: [
      {
        test: /\.js$/,
        use: {
          loader: 'swc-loader',
          options: {
            jsc: {
              parser: {
                syntax: 'ecmascript'
              },
              transform: {
                react: {
                  runtime: 'automatic'
                }
              }
            }
          }
        }
      }
    ]
  }
}
```

### 缩小编译范围

```javascript
module.exports = {
  module: {
    rules: [
      {
        test: /\.js$/,
        include: path.resolve(__dirname, 'src'),
        exclude: /node_modules/,
        use: 'babel-loader'
      }
    ]
  },
  resolve: {
    alias: {
      '@': path.resolve(__dirname, 'src')
    }
  }
}
```

## 产物优化

### 优化策略对比

| 策略 | 说明 | 效果 |
|------|------|------|
| **代码分割** | splitChunks、动态 import | 减少首屏加载 |
| **Tree Shaking** | 移除无用代码 | 减小体积 |
| **Scope Hoisting** | 作用域提升 | 减少闭包 |
| **压缩** | Terser、cssnano | 减小体积 |

### 代码分割

**Webpack splitChunks**
```javascript
module.exports = {
  optimization: {
    splitChunks: {
      chunks: 'all',
      minSize: 20000,
      maxSize: 244000,
      cacheGroups: {
        vendors: {
          test: /[\\/]node_modules[\\/]/,
          name: 'vendors',
          chunks: 'all'
        },
        common: {
          name: 'common',
          minChunks: 2,
          chunks: 'all'
        }
      }
    }
  }
}
```

**动态 import**
```javascript
// 懒加载组件
const Home = () => import('./views/Home.vue')
const About = () => import('./views/About.vue')

// Vue Router
const routes = [
  { path: '/', component: () => import('./views/Home.vue') }
]
```

**Vite manualChunks**
```javascript
export default defineConfig({
  build: {
    rollupOptions: {
      output: {
        manualChunks: {
          vendor: ['vue', 'vue-router', 'pinia'],
          ui: ['element-plus'],
          utils: ['lodash', 'dayjs']
        }
      }
    }
  }
})
```

### Tree Shaking

**确保 ES Module 格式**
```json
// package.json
{
  "sideEffects": false
}
```

**按需导入**
```javascript
// ❌ 不推荐
import _ from 'lodash'

// ✅ 推荐
import debounce from 'lodash/debounce'
import { debounce } from 'lodash-es'
```

### 压缩配置

**Terser（JavaScript）**
```javascript
module.exports = {
  optimization: {
    minimize: true,
    minimizer: [
      new TerserPlugin({
        parallel: true,
        terserOptions: {
          compress: {
            drop_console: true
          }
        }
      })
    ]
  }
}
```

**cssnano（CSS）**
```javascript
const CssMinimizerPlugin = require('css-minimizer-webpack-plugin')

module.exports = {
  optimization: {
    minimizer: [
      new CssMinimizerPlugin()
    ]
  }
}
```

**Gzip 压缩**
```javascript
const CompressionPlugin = require('compression-webpack-plugin')

module.exports = {
  plugins: [
    new CompressionPlugin({
      algorithm: 'gzip',
      test: /\.(js|css|html|svg)$/,
      threshold: 10240,
      minRatio: 0.8
    })
  ]
}
```

## 构建分析

### Webpack 分析

```bash
# 生成构建分析文件
npm run build -- --profile --json > stats.json

# 使用 webpack-bundle-analyzer
npx webpack-bundle-analyzer stats.json

# 使用在线工具
# https://webpack.github.io/analyse/
```

### Vite 分析

```bash
# 使用 vite-bundle-visualizer
npx vite-bundle-visualizer

# 或使用 rollup-plugin-visualizer
```

```javascript
// vite.config.js
import { visualizer } from 'rollup-plugin-visualizer'

export default defineConfig({
  plugins: [
    visualizer({ open: true })
  ]
})
```

### Rollup 分析

```javascript
import { visualizer } from 'rollup-plugin-visualizer'

export default {
  plugins: [
    visualizer({
      filename: './stats.html',
      open: true
    })
  ]
}
```

## 优化检查清单

### 开发环境
- [ ] 开启 HMR
- [ ] 配置缓存
- [ ] 缩小编译范围
- [ ] 使用 esbuild/swc 替代 Babel
- [ ] 配置 devtool（source-map）

### 生产环境
- [ ] 代码分割配置
- [ ] Tree Shaking 配置
- [ ] 压缩配置
- [ ] Gzip/Brotli 压缩
- [ ] 移除 console/debugger
- [ ] 图片/资源优化
- [ ] 构建分析验证
