# 核心原理详解

深入理解 Webpack 和 Vite 的核心工作原理。

---

## Webpack 核心原理

### 1. 模块打包机制

Webpack 将所有模块打包成一个或多个 Bundle：

```javascript
// 简化的 Bundle 结构
(function(modules) {
  // 模块缓存
  var installedModules = {};
  
  // require 函数
  function __webpack_require__(moduleId) {
    // 检查缓存
    if (installedModules[moduleId]) {
      return installedModules[moduleId].exports;
    }
    
    // 创建新模块并缓存
    var module = installedModules[moduleId] = {
      i: moduleId,
      l: false,
      exports: {}
    };
    
    // 执行模块函数
    modules[moduleId].call(module.exports, module, module.exports, __webpack_require__);
    
    // 标记已加载
    module.l = true;
    
    return module.exports;
  }
  
  // 加载入口模块
  return __webpack_require__(__webpack_entry__);
})([
  /* 模块数组 */
  function(module, exports, __webpack_require__) {
    // 模块 0 的代码
  },
  function(module, exports, __webpack_require__) {
    // 模块 1 的代码
  }
]);
```

### 2. 依赖图构建

```javascript
// Webpack 依赖图构建过程
class DependencyGraph {
  constructor() {
    this.graph = new Map()
  }
  
  // 从入口开始构建
  buildFromEntry(entry) {
    const entryModule = this.buildModule(entry)
    this.graph.set(entry, entryModule)
    
    // 递归处理依赖
    this.processDeps(entryModule.dependencies)
  }
  
  // 编译单个模块
  buildModule(modulePath) {
    let content = fs.readFileSync(modulePath, 'utf-8')
    
    // 应用 Loader
    content = this.applyLoaders(content, modulePath)
    
    // 提取依赖
    const dependencies = this.parseDependencies(content)
    
    return { modulePath, content, dependencies }
  }
  
  // 解析依赖
  parseDependencies(content) {
    const ast = parse(content)
    const dependencies = []
    
    traverse(ast, {
      ImportDeclaration({ node }) {
        dependencies.push(node.source.value)
      },
      CallExpression({ node }) {
        if (node.callee.name === 'require') {
          dependencies.push(node.arguments[0].value)
        }
      }
    })
    
    return dependencies
  }
}
```

### 3. HMR 原理

```javascript
// Webpack HMR 流程
// 1. 服务端监听文件变化
watcher.on('change', (file) => {
  // 2. 重新编译变化的模块
  const updatedModules = recompile(file)
  
  // 3. 生成更新 manifest
  const manifest = {
    a: { changed: true, hash: 'abc123' },
    b: { changed: true, hash: 'def456' }
  }
  
  // 4. 通过 WebSocket 通知客户端
  ws.send({ type: 'hash', hash: newHash })
  ws.send({ type: 'ok' })
})

// 5. 客户端接收更新
if (module.hot) {
  module.hot.accept('./component', () => {
    // 执行更新回调
    render(App)
  })
}
```

### 4. Tapable 钩子系统

```javascript
// Webpack 插件系统基于 Tapable
const { SyncHook, AsyncSeriesHook } = require('tapable')

class Compiler {
  constructor() {
    // 定义各种钩子
    this.hooks = {
      run: new AsyncSeriesHook(['compiler']),
      compile: new SyncHook(['params']),
      done: new AsyncSeriesHook(['stats'])
    }
  }
  
  // 触发钩子
  run(callback) {
    this.hooks.run.callAsync(this, err => {
      this.compile(onCompiled)
    })
  }
}

// 插件注册
compiler.hooks.done.tap('MyPlugin', (stats) => {
  console.log('Build done!')
})
```

---

## Vite 核心原理

### 1. ESM 原生支持

Vite 利用浏览器原生 ES Module 能力：

```html
<!-- index.html -->
<script type="module" src="/src/main.js"></script>
```

```javascript
// main.js - 浏览器直接加载
import { createApp } from '/node_modules/.vite/deps/vue.js'
import App from '/src/App.vue'

createApp(App).mount('#app')
```

### 2. 依赖预构建

Vite 会预先构建依赖：

```javascript
// 预构建的目的：
// 1. CommonJS/UMD → ESM 转换
// 2. 减少请求数（合并小模块）

// vite.config.js
export default {
  optimizeDeps: {
    include: ['lodash-es', 'axios'],
    exclude: ['your-local-package']
  }
}

// 预构建产物存储在 node_modules/.vite/deps/
```

### 3. 按需编译

```javascript
// Vite 按需编译中间件
export function transformMiddleware(server) {
  return async (req, res, next) => {
    const url = req.url
    
    // 1. 检查是否是需要处理的文件
    if (!shouldTransform(url)) {
      return next()
    }
    
    // 2. 加载文件
    const file = await server.moduleGraph.getModuleByUrl(url)
    
    // 3. 使用 esbuild 编译
    const result = await transformWithEsbuild(
      file.code,
      file.id,
      { loader: getFileLoader(file.id) }
    )
    
    // 4. 返回编译结果
    res.setHeader('Content-Type', 'application/javascript')
    res.end(result.code)
  }
}
```

### 4. HMR 实现

```javascript
// Vite HMR 实现
// 服务端
const hmrClient = `
  const ws = new WebSocket('ws://localhost:5173')
  
  ws.addEventListener('message', ({ data }) => {
    const { type, path } = JSON.parse(data)
    
    if (type === 'update') {
      // 动态导入更新后的模块
      import(path + '?t=' + Date.now())
    }
  })
`

// 客户端注入
server.middlewares.use((req, res, next) => {
  if (req.url === '/@vite/client') {
    res.setHeader('Content-Type', 'application/javascript')
    res.end(hmrClient)
  }
})

// HMR 边界处理
// Vue 组件
if (import.meta.hot) {
  import.meta.hot.accept((newModule) => {
    // 重新渲染组件
    __VUE_HMR_RUNTIME__.reload(componentId, newModule.default)
  })
}
```

### 5. 模块依赖图

```javascript
// Vite 模块图管理
class ModuleGraph {
  constructor() {
    this.urlToModuleMap = new Map()
    this.idToModuleMap = new Map()
  }
  
  // 通过 URL 获取模块
  getModuleByUrl(url) {
    return this.urlToModuleMap.get(url)
  }
  
  // 更新模块信息
  updateModuleInfo(module, importedModules) {
    module.importedModules = new Set(importedModules)
    
    // 更新反向依赖
    importedModules.forEach(imported => {
      imported.importers.add(module)
    })
  }
  
  // HMR 边界检测
  getHmrBoundary(modules) {
    // 找到最近的 accept 边界
    // 返回需要更新的模块链
  }
}
```

---

## 关键技术对比

### 编译器

| 工具 | 编译器 | 特点 |
|------|--------|------|
| Webpack | acorn + babel | 慢但兼容性好 |
| Vite | esbuild | 极快，Go 编写 |

### 文件监听

| 工具 | 监听方式 | 特点 |
|------|----------|------|
| Webpack | chokidar | 全量监听 |
| Vite | chokidar + 按需 | 更精准 |

### 缓存策略

| 工具 | 缓存方式 | 特点 |
|------|----------|------|
| Webpack | filesystem cache | 需要配置 |
| Vite | 预构建 + 内存 | 默认开启 |

---

[返回上级](./)
