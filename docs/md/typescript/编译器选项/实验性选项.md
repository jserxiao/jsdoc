# 实验性选项

TypeScript 提供了一些实验性选项，这些特性可能在未来版本中发生变化。

---

## 目录

- [装饰器](#装饰器)
- [元数据](#元数据)
- [其他实验性选项](#其他实验性选项)

---

## 装饰器

### experimentalDecorators

启用装饰器支持：

```json
{
  "compilerOptions": {
    "experimentalDecorators": true
  }
}
```

```typescript
// 类装饰器
function sealed(constructor: Function) {
  Object.seal(constructor);
  Object.seal(constructor.prototype);
}

@sealed
class Greeter {
  greeting: string;
  constructor(message: string) {
    this.greeting = message;
  }
}

// 方法装饰器
function log(
  target: any,
  propertyKey: string,
  descriptor: PropertyDescriptor
) {
  const original = descriptor.value;
  descriptor.value = function(...args: any[]) {
    console.log(`Calling ${propertyKey} with`, args);
    return original.apply(this, args);
  };
  return descriptor;
}

class Calculator {
  @log
  add(a: number, b: number) {
    return a + b;
  }
}

// 属性装饰器
function readonly(target: any, propertyKey: string) {
  Object.defineProperty(target, propertyKey, {
    writable: false
  });
}

class Person {
  @readonly
  name: string = 'Alice';
}

// 参数装饰器
function validate(
  target: any,
  propertyKey: string,
  parameterIndex: number
) {
  console.log(`Parameter ${parameterIndex} of ${propertyKey} validated`);
}

class Service {
  process(@validate data: string) {
    // ...
  }
}
```

### 装饰器工厂

```typescript
function configurable(value: boolean) {
  return function(target: any, propertyKey: string) {
    Object.defineProperty(target, propertyKey, {
      configurable: value
    });
  };
}

class Point {
  @configurable(false)
  x: number = 0;
}
```

### 装饰器组合

```typescript
function first() {
  console.log('first(): factory evaluated');
  return function(target: any, propertyKey: string, descriptor: PropertyDescriptor) {
    console.log('first(): called');
  };
}

function second() {
  console.log('second(): factory evaluated');
  return function(target: any, propertyKey: string, descriptor: PropertyDescriptor) {
    console.log('second(): called');
  };
}

class Example {
  @first()
  @second()
  method() {}
}

// 输出:
// first(): factory evaluated
// second(): factory evaluated
// second(): called
// first(): called
```

---

## 元数据

### emitDecoratorMetadata

启用装饰器元数据支持：

```json
{
  "compilerOptions": {
    "experimentalDecorators": true,
    "emitDecoratorMetadata": true
  }
}
```

需要安装 `reflect-metadata`：

```bash
npm install reflect-metadata
```

```typescript
import 'reflect-metadata';

const formatMetadataKey = Symbol('format');

function format(formatString: string) {
  return Reflect.metadata(formatMetadataKey, formatString);
}

function getFormat(target: any, propertyKey: string) {
  return Reflect.getMetadata(formatMetadataKey, target, propertyKey);
}

class Greeter {
  @format('Hello, %s')
  greeting: string;

  constructor(message: string) {
    this.greeting = message;
  }

  greet() {
    const formatString = getFormat(this, 'greeting');
    return formatString.replace('%s', this.greeting);
  }
}

// 类型元数据
function ParamTypes(target: any, key: string) {
  const types = Reflect.getMetadata('design:paramtypes', target, key);
  console.log(types);  // [Number, String]
}

class Foo {
  @ParamTypes
  doSomething(n: number, s: string): boolean {
    return true;
  }
}
```

### 元数据 API

```typescript
import 'reflect-metadata';

// 定义元数据
Reflect.defineMetadata('custom:metadata', 'value', target, propertyKey);

// 获取元数据
const value = Reflect.getMetadata('custom:metadata', target, propertyKey);

// 检查元数据是否存在
const hasMetadata = Reflect.hasMetadata('custom:metadata', target, propertyKey);

// 获取所有元数据键
const keys = Reflect.getMetadataKeys(target, propertyKey);

// 删除元数据
Reflect.deleteMetadata('custom:metadata', target, propertyKey);
```

---

## 其他实验性选项

### emitDeclarationOnly

只输出声明文件：

```json
{
  "compilerOptions": {
    "emitDeclarationOnly": true
  }
}
```

适用于打包工具处理 JavaScript 编译的场景。

### emitDecoratorMetadata 已弃用

> 注意：`emitDecoratorMetadata` 在 TypeScript 5.0 中已被标记为弃用，建议使用新的装饰器提案。

### 新装饰器提案 (TypeScript 5.0+)

```typescript
// 新装饰器语法
function logged(value: Function, context: ClassMethodDecoratorContext) {
  return function(this: any, ...args: any[]) {
    console.log(`Calling ${String(context.name)}`);
    return value.apply(this, args);
  };
}

class Calculator {
  @logged
  add(a: number, b: number) {
    return a + b;
  }
}
```

---

## 常见框架使用

### NestJS

```json
{
  "compilerOptions": {
    "experimentalDecorators": true,
    "emitDecoratorMetadata": true
  }
}
```

```typescript
import { Controller, Get, Post, Body } from '@nestjs/common';

@Controller('users')
export class UsersController {
  @Get()
  findAll(): User[] {
    return [];
  }

  @Post()
  create(@Body() createUserDto: CreateUserDto): User {
    return {} as User;
  }
}
```

### TypeORM

```typescript
import { Entity, PrimaryGeneratedColumn, Column } from 'typeorm';

@Entity()
export class User {
  @PrimaryGeneratedColumn()
  id: number;

  @Column()
  firstName: string;

  @Column()
  lastName: string;
}
```

### Inversify (依赖注入)

```typescript
import { injectable, inject } from 'inversify';

@injectable()
class Ninja implements Warrior {
  @inject(TYPES.Weapon) private weapon: Weapon;

  fight() {
    return this.weapon.hit();
  }
}
```

---

## 配置示例

### 使用装饰器的完整配置

```json
{
  "compilerOptions": {
    "target": "ES2020",
    "module": "ESNext",
    "moduleResolution": "node",
    "experimentalDecorators": true,
    "emitDecoratorMetadata": true,
    "strict": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "sourceMap": true
  }
}
```

---

**返回**: [编译器选项目录](./index.md)
